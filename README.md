# Job Search (Java Desktop App)

A simple Java desktop application for managing job listings and companies.  
It uses Java Swing for the GUI and serialized files for storing users, tickets, and company data.

## Features

- User login form
- List of job tickets (positions, salary range, city, type)
- List of companies with basic information
- Simple persistence using serialized Java objects (`database`, `companies database`)
